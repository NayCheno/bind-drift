# Missing Case Types

Selected positive drift types: SignatureDrift.

The following target drift types were not represented by an adjudicated positive case:
- `NullabilityDrift`
- `OwnershipRefcountDrift`
- `AllocationFreeDrift`
- `SleepabilityContextDrift`
- `LayoutFieldDrift`

Semantic true-positive case coverage is below the two-case gate, so semantic claims must remain exploratory.
